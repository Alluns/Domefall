using Managers;
using UnityEngine;

namespace UI
{
    public class OptionsUI : MonoBehaviour, IMenu
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
